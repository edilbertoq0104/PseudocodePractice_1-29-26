DECLARE interger variable text = userInput
DECLARE interger variable capText = 
DECLARE boolean variable done = false

WHILE not done
    PRINT "Please enter text to capitalize"
    INPUT text
    IF userInput has digits THEN
        PRINT "Please only use text"    
    ELSE
      PRINT "Capitalizing your text!"
    END IF
    OUTPUT capText





2.

DECLARE interger variable number1 = 0
DECLARE interger variable number2 = 0
DECLARE interger variable
